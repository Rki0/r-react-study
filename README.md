# Basic TO-DO List

Please implement CRUD features for TO-DO List app!

# Features

## Create

1. Should use array(`[]`).
2. Should use `useState()`.
3. Should use `onChange` handler.
4. Should use `onClick` handler.

## Read (=== Render)

1. Should render using the `map()`.

## Update

## Delete

그냥 기능 명세를 만드는게 더 낫지 않을까?
